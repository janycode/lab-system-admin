<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jerry.labsystem.dao.RightMapper">
    <resultMap id="menuTree" type="com.jerry.labsystem.pojo.Right">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="path" property="path"/>
        <result column="icon" property="icon"/>
        <!-- 将 children 列表通过 select 方法注入进来 -->
        <collection property="children" select="getMenuChildren" ofType="com.jerry.labsystem.pojo.Right" column="id"/>
    </resultMap>

    <select id="getMenuChildren" resultMap="menuTree">
        SELECT id, title, path, icon
        FROM rights
        WHERE parent_id = #{parentId}
    </select>

    <select id="getRightList" resultMap="menuTree">
        SELECT *
        FROM rights
        WHERE parent_id = 0
    </select>

    <update id="updateRightList">
        update rights
        <set>
            <if test="title != null">
                title = #{title},
            </if>
            <if test="icon != null">
                icon = #{icon}
            </if>
        </set>
        where id = #{id}
    </update>

    <delete id="deleteRightList">
        DELETE
        FROM rights
        WHERE id = #{id}
    </delete>
</mapper>
